#include <stdio.h>
int main(){
    int a,b,c,d,i,j,k,sum=0;
    char x[101];
    scanf("%d",&a);
    while(a--){
        scanf("%d",&b);
        for(j=0;j<b;j++){
            scanf("%s",&x);
            c=strlen(x);
            for(k=0;k<c;k++){
                sum=sum+(x[k]-65)+j+k;
            }
        }
        printf("%d\n",sum);
    sum=0;
    }
    return 0;
}
