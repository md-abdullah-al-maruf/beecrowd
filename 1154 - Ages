#include<stdio.h>

int main()
{
    int a=1,b=0,i=0;
    float avr;
    while(1) {
        scanf("%d",&a);
        if(a>0) {
            b=b+a;
            i++;
        }
        else {
            break;
        }
    }
    avr=(float)b/i;
    printf("%.2f\n",avr);
    return 0;
}
