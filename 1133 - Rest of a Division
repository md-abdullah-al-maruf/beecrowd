#include<stdio.h>
int main()
{
    int a,b,i,x=0;
    scanf("%d%d",&a,&b);
    if(a<b)
    {
        for(i=a+1; i<b; i++)
        {
            x=(i%5);
            if(x==2||x==3)
            {
                printf("%d\n",i);
                x=0;
            }
        }

    }
    else if(a>b){
        for(i=b+1; i<a; i++)
        {
            x=(i%5);
            if(x==2||x==3)
            {
                printf("%d\n",i);
                x=0;
            }
        }

    }

    return 0;
}
