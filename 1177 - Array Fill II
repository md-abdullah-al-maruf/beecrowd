#include<stdio.h>

int main()
{
    int a[1000],b,i,j,k;
    scanf("%d",&b);
    for(i=0,k=0; i<1000; i++,k++) {
        if(k==b) {
            k=0;
        }
        a[i]=k;
    }
    for(j=0; j<1000; j++) {
        printf("N[%d] = %d\n",j,a[j]);
    }
    return 0;
}
