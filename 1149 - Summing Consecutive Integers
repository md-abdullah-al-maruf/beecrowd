#include<stdio.h>
int main()
{
    int a,b,i,sum=0;
    scanf("%d%d",&a,&b);
aa:
    if(b<=0)
    {
        scanf("%d",&b);
        goto aa;
    }
    for(i=1; i<=b; i++)
    {
        sum=sum+a;
        a++;
    }
    printf("%d\n",sum);
    return 0;
}
