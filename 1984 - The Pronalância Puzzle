#include<stdio.h>
#include<string.h>
int main()
{
    int b,i,j;
    char a[100000];
    scanf("%s",&a);
    b=strlen(a);
    for(i=b-1;i>=0;i--){
        if(i==0){
            printf("%c\n",a[i]);
        }
        else{
        printf("%c",a[i]);
        }
    }
    return 0;
}
