#include<stdio.h>
int main()
{
    int a,b,c,i=0;
    float x;
    scanf("%d",&c);
    while(i<c){
        scanf("%d%d",&a,&b);
        if(b!=0){
            x=(float)a/(float)b;
            printf("%.1f\n",x);
        }
        else{
            printf("divisao impossivel\n");
        }
        i++;
    }
    return 0;
}
