#include<stdio.h>
int main()
{
    int a,b,c,d=0,i,j=0,sum=0;
    scanf("%d",&c);
    while(d<c)
    {
        scanf("%d%d",&a,&b);
        for(i=0; i<(b*2); i++)
        {
            if(a%2!=0)
            {
                sum=sum+a;
            }
            a++;
        }
        printf("%d\n",sum);
        sum=0;
        d++;
    }
    return 0;
}
